import styled from '@emotion/styled'
import { space, SpaceProps } from 'styled-system'

export const CompanyLinksWrapper = styled.div`
  ${space}
`

export const InlineAnchor = styled.a<SpaceProps>`
  display: inline-block;
  ${space}
`
